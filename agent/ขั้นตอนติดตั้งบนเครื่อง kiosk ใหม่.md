ขั้นตอนติดตั้งบนเครื่อง kiosk ใหม่:

1.Copy folder nhso_agent\ ไปวางบนเครื่อง (ยังไม่มี python\)
2.แก้ config.ini — ตั้ง server_url
3.double-click build_agent.bat — รอ download + install (~2-3 นาที)
4.double-click run_agent.bat — ใช้งานได้เลย